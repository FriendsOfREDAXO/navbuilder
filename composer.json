{
    "name": "friendsofredaxo/navbuilder",
    "description": "Dev tooling for the NavBuilder REDAXO addon — the addon itself ships without dependencies.",
    "license": "MIT",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "scripts": {
        "test": "phpunit"
    }
}
